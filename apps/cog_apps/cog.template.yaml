# Configuration for Cog ⚙️
# Reference: https://cog.run/yaml

build:
  gpu: true
  cuda: "13.0"

  python_version: "3.12"
  system_packages:
    - "git"

  python_requirements: requirements.txt

  # commands run after the environment is setup
  run:
    - pip install https://github.com/ModelCloud/GPTQModel/releases/download/v5.6.12/gptqmodel-5.6.12+cu130torch2.9-cp312-cp312-linux_x86_64.whl
    - curl -o /usr/local/bin/pget -L "https://github.com/replicate/pget/releases/download/v0.11.0/pget_linux_x86_64" && chmod +x /usr/local/bin/pget

predict: "app.py:Model"
